Activity
    .joins(:activity_records, :user)
    .select("'activities'.id, title, point, COUNT('activity_records'.id) AS today_activity_records_count")
    .group("activities.id")
    .where(user_id: your_desired_user_id_here)


.where(people: {position_status: "graduate student"}).where(departments_people: {department_id: 11}).order('people.last_name').uniq

	#@person_dept_name = Person.join(:departments_people).join(:department).select("departments.name")
